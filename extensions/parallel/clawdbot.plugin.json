{
  "id": "parallel-search",
  "name": "Parallel.ai",
  "version": "1.1.0",
  "description": "Web search and content extraction powered by Parallel.ai (parallel_web_search, parallel_extract)",
  "entry": "index.ts",
  "uiHints": {
    "apiKey": {
      "label": "Parallel.ai API Key",
      "sensitive": true,
      "placeholder": "Get from platform.parallel.ai",
      "help": "API key from Parallel.ai (or use ${PARALLEL_API_KEY})"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Parallel.ai API key"
      }
    },
    "required": ["apiKey"]
  }
}
